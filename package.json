{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "config": {
    "ghooks": {
      "pre-commit": "yarn test",
      "pre-push": "yarn run health-check"
    }
  },
  "author": "",
  "license": "ISC"
}

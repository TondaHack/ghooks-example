{
  "name": "git-hooks-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "echo": "echo \"Everything is ok\""
  },
  "config": {
    "ghooks": {
      "pre-commit": "yarn echo",
      "pre-push": "yarn echo",
      "post-merge": "yarn echo"
    }
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ghooks": "^2.0.0"
  }
}
